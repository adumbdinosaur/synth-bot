# Use nix-shell for development environment
use nix

# Load .env file if it exists
dotenv_if_exists

# Add project root to Python path
PATH_add "."

# Set up environment for the project
export TELEGRAM_USERBOT_DEV=1

# Note: Virtual environment activation is handled by shell.nix
